# Image de développement : sert l'application via le dev-server Angular (hot reload).
# Pour la production, utiliser le `Containerfile` (build + nginx).
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --legacy-peer-deps

COPY . .

EXPOSE 4200

CMD ["npm", "start"]
